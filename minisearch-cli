#!/usr/bin/env python3
"""Executable wrapper so the CLI can run as ./minisearch-cli or on PATH."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from minisearch.cli import main

if __name__ == "__main__":
    sys.exit(main())
