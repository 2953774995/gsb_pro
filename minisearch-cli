#!/usr/bin/env python3
"""minisearch-cli 启动脚本（等价于 python3 -m minisearch）。"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from minisearch.cli import main

if __name__ == "__main__":
    sys.exit(main())
