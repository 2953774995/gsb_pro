#!/usr/bin/env python3
"""Thin launcher so ``./rex-cli`` works from the repository root."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from rex.cli import main

if __name__ == "__main__":
    sys.exit(main())
