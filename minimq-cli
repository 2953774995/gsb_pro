#!/usr/bin/env python3
"""Convenience launcher so ``./minimq-cli`` works without installing."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from minimq.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
