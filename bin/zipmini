#!/usr/bin/env python3
"""Executable wrapper so `./zipmini ...` works from the repo root."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from zipmini.cli import main

sys.exit(main())
