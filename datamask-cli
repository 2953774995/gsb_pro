#!/usr/bin/env python3
"""datamask-cli 启动脚本（无需安装，直接在仓库根目录运行）。"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from datamask.cli import main

if __name__ == "__main__":
    sys.exit(main())
