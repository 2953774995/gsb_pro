# 语法清单

## 标题与段落

`#` 到 `######` 对应 `<h1>` 到 `<h6>`，空行分隔段落。

## 行内样式

**粗体**、*斜体*、`行内代码`，还有 [链接](https://example.com) 和图片：

![占位图](../assets/img.png)

## 列表

1. 有序第一项
2. 有序第二项
   - 嵌套无序
   - 再嵌套一层

## 表格

| 元素 | 语法 | 备注 |
| --- | --- | --- |
| 粗体 | `**x**` | 行内 |
| 表格竖线 | `a \| b` | 单元格里的 `\|` 要转义 |

## 代码块

```python
def hello(name: str) -> None:
    # 关键词、字符串、注释都会着色
    print(f"hello, {name}")
```

## 引用与水平线

> 引用块里也可以用 **行内样式**。

---

## 原生 HTML

行内 <kbd>Ctrl</kbd> + <kbd>C</kbd> 原样透传，块级 HTML 也一样：

<div class="note">这是一段原样输出的块级 HTML。</div>
