# datamask

A sensitive-data detection and masking tool for customer-support tickets with a self-built pattern matcher, Python standard library only.

See PRD in the task prompt for full requirements.
